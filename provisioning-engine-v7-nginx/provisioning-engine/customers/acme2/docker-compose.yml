services:
  odoo:
    image: odoo:19.0
    container_name: odoo-acme2
    user: root
    tty: true
    depends_on:
      db:
        condition: service_healthy
    environment:
      - HOST=db
      - USER=odoo
      - PASSWORD=odoo
    restart: always
    ports:
      - "7020:8069"
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 2g
        reservations:
          cpus: "0.5"
          memory: 1g
    networks:
      net-acme2:
        aliases:
          - odoo
      proxy-net:
        aliases:
          - acme2
    volumes:
      - data:/var/lib/odoo
      - config:/etc/odoo
      - ./addons:/mnt/extra-addons
      - log:/var/log/odoo
    logging:
      options:
        max-size: "10m"
        max-file: "3"

  db:
    image: postgres:16
    container_name: postgres-acme2
    environment:
      - POSTGRES_USER=odoo
      - POSTGRES_PASSWORD=odoo
      - POSTGRES_DB=postgres
    restart: always
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512m
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U odoo"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - ./postgresql:/var/lib/postgresql/data
    networks:
      - net-acme2
    logging:
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  data:
  config:
  log:

networks:
  net-acme2:
    driver: bridge
  proxy-net:
    external: true